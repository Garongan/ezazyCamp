import React from "react";

const RegisterGuideScreen = () => {
    return (
        <View
            style={[
                {
                    flex: 1,
                    backgroundColor: theme.colors.background,
                    justifyContent: "center",
                    alignItems: "center",
                },
                theme.padding,
            ]}
        >
            <Text>Register Guide Screen</Text>
        </View>
    );
};

export default RegisterGuideScreen;
